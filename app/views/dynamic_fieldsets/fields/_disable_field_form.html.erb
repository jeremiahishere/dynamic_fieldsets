<%= form_tag dynamic_fieldsets_enable_field_path(field), :method => :post do %>
  <%= hidden_field_tag "dynamic_fieldsets_field[enabled]", !field.enabled %>
  <%= submit_tag "#{field.enabled ? 'Disable' : 'Enable'}" %>
<% end %>
